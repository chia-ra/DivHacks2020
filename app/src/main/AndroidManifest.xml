<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.thinkgreen">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ThinkGreen">
        <activity android:name=".GlassRecycle"
            android:parentActivityName=".RecycleActivity" />
        <activity
            android:name=".MetalReuse"
            android:parentActivityName=".ReuseActivity" />
        <activity
            android:name=".PlasticRecycle"
            android:parentActivityName=".RecycleActivity" />
        <activity
            android:name=".MetalRecycle"
            android:parentActivityName=".RecycleActivity" />
        <activity
            android:name=".paperRecycle"
            android:parentActivityName=".RecycleActivity" />
        <activity
            android:name=".RecycleActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".ReuseActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>